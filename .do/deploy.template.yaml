spec:
  name: mari-medical
  services:
    - name: app
      git:
        branch: main
        repo_clone_url: https://github.com/cfu288/mari-medical.git
      envs:
        - key: ONPATIENT_CLIENT_ID
          type: SECRET
        - key: ONPATIENT_CLIENT_SECRET
          type: SECRET
        - key: PUBLIC_URL
          value: ${APP_URL}
