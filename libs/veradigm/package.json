{
  "name": "@mere/veradigm",
  "version": "0.0.1",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "tsx": "^4.21.0"
  },
  "scripts": {
    "fetch-endpoints": "tsx fetchMetadata.ts --openssl-config=openssl.cnf --no-experimental-fetch"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
