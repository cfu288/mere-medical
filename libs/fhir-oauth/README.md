# fhir-oauth

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fhir-oauth` to build the library.

## Running unit tests

Run `nx test fhir-oauth` to execute the unit tests via [Jest](https://jestjs.io).
